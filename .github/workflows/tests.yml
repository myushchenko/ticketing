name: tests

on: 
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd ./auth
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm run test:ci
